<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <%=  link_to "Homzhub", root_path, class: "navbar-brand" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-brand"> Search | Menu </span>
  </button>
  <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "View users", admin_users_path, class: "nav-link" %>
        </li>
        <li class="nav-item ">
          <a class="nav-link disabled" href="#">Disabled</a>
        </li>
        <li>
          <% if current_user %>
            <%= link_to "Upload a property", new_property_path, class: "btn btn-warning" %>
          <% else %>
          <%=  link_to "Upload a property", new_user_registration_path, class: "btn btn-warning", notice: "You need to sign up first"%>
          <% end %>
        </li>
        <hr class="d-md-none">
      </ul>
      <div class="navig-pad">
          <%= render "shared/search_home" %>  
      </div>
          <%= render "shared/usercontrol" %>
  </div>
</nav>
